import 'package:flutter/material.dart';

/// Stub — replaced by Task 16.
class MedicineFormScreen extends StatelessWidget {
  /// Creates the stub.
  const MedicineFormScreen({super.key, this.editMedicineId});

  /// Id of the medicine being edited, or `null` when creating a new one.
  final String? editMedicineId;

  @override
  Widget build(BuildContext context) => const Placeholder();
}
