import 'package:flutter/material.dart';

/// Stub — replaced by a later task.
class PrayerQadhaScreen extends StatelessWidget {
  /// Creates the stub.
  const PrayerQadhaScreen({super.key});

  @override
  Widget build(BuildContext context) => const Placeholder();
}
