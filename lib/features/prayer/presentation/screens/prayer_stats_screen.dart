import 'package:flutter/material.dart';

/// Stub — replaced by a later task.
class PrayerStatsScreen extends StatelessWidget {
  /// Creates the stub.
  const PrayerStatsScreen({super.key});

  @override
  Widget build(BuildContext context) => const Placeholder();
}
