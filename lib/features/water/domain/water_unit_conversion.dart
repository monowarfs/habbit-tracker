/// Milliliters per US customary fluid ounce (D-01) — the single source of
/// truth shared by display formatting and natural-language quick-add
/// parsing, so the two never drift apart.
const mlPerFlOz = 29.5735;
